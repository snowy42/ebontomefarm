# 1.0.2 work checkpoint

Matthew's in-game feedback: accidental manual ticks cannot be reset together;
Replan can send an Eastern Kingdoms player to ICC/Crystalsong; request a
prominent TOME FOUND notification on bag acquisition. Continue from v1.0.1
commit d8993c7c77a523cb42d2e4a64c76352dc30ee043, preserving saved variables.

Inspected causes: Replan never clears manual collection overrides. The old
route orders weighted target coverage divided by distance, and can select a
large remote stop with no position sample, then hold it as active. The polling
scan runs before the navigation position sample, making this possible during
loading/zone transitions. The new target labels did not modify that algorithm.

Implementation in progress:
- Confirmed Reset build button (also /etf reset): clear manual done/need only
  for this build, restore skipped stops, rescan discoveries/spellbook/bags,
  preserve imported build, real unlocks, personal pins and unrelated marks.
- Distance-first source choice. After selecting the nearest stop, finish its
  native map zone before choosing the next nearest zone. Replan starts fresh
  from the player's safe current position. Preserve active farms until their
  wanted tomes are obtained. Prefer the current continent; no fabricated
  cross-continent distances. Wait for a valid position instead of guessing.
- Lightweight bag polling plus existing bag/loot events. Silent login baseline,
  session deduplication for bag moves/repeated scans, queued on-screen tome
  names with optional sound, including while tracker is hidden/route paused.
- Regression tests for all-marked reset, confirmation cancellation, partial
  learned/bag ownership, zone grouping, missing position, moving between stops,
  nearest versus many-target raids, and notification baseline/dedup/queue.

Do not claim this patch is released until the implementation and tests are
committed and the versioned release artifact has been verified. The local
working tree during this continuation is /mnt/data/etf102/src; it is not a
required dependency and final work must be recoverable from main.
